To run this application:

1) start yarn run json
   You must have json-server installed on your system:
      npm install -g json-server
2) start yarn run serve
3) Open browser to http://localhost:8080

